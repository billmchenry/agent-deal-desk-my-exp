

## Update Confetti Colors

Single change in `src/components/shared/ConfettiCelebration.tsx` — update the `CONFETTI_COLORS` array from the current values to the new palette:

```ts
const CONFETTI_COLORS = ["#E9EBF6", "#6065AE", "#989ECB", "#4142A3"];
```

